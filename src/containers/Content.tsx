import { css } from "@emotion/react";

const style = css`
    height: 100%;
`

const Content = () => {
    return (
        <section css={style}>
            b
        </section>
    );
}

export default Content