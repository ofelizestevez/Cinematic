import { css } from "@emotion/react";

const style = css`
    height: 40vh;
`

const Header = () => {
    return (
        <header css={style}>
            a
        </header>
    );
}

export default Header